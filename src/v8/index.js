import{STRATEGY,TECH,num,pct,int,bool,round,isBotOrder,etParts,entryWindowOpen,inConfiguredBlackout}from'./config.js';
import{alpaca,dynamicUniverse,fetchBars,fetchSnapshots,recentNewsRisk,preferredFeed}from'./api.js';
import{timeframeSignal,combineSignals,marketRegime}from'./signals.js';
import{calculatePerformanceDetailed,botInventoryFromOrders,todayPerformance,recentCooldowns,adaptiveLearning}from'./performance.js';
import{cancelStaleBotOrders,exitDecision,placeLimitSell,placeLimitBuy,positionNotional}from'./execution.js';

async function runCycle(env,scheduledTime){
  if(!env.APCA_API_KEY_ID||!env.APCA_API_SECRET_KEY)throw new Error('missing_alpaca_secrets');
  if(String(env.TRADING_ENABLED)!=='true')return{status:'disabled',endpoint:'paper',strategy:STRATEGY};
  const clock=await alpaca(env,'/v2/clock');if(!clock.is_open)return{status:'market_closed',endpoint:'paper',strategy:STRATEGY};
  const symbols=await dynamicUniverse(env),snap=await fetchSnapshots(env,symbols),bundles=await Promise.all(symbols.map(async symbol=>{try{const[m1,m5,m15]=await Promise.all([fetchBars(env,symbol,'1Min',55),fetchBars(env,symbol,'5Min',80),fetchBars(env,symbol,'15Min',70)]);return[symbol,combineSignals(symbol,timeframeSignal(symbol,m1.bars),timeframeSignal(symbol,m5.bars),timeframeSignal(symbol,m15.bars),snap.snapshots[symbol])]}catch(e){console.log(JSON.stringify({event:'symbol_data_failed',symbol,message:e.message}));return[symbol,{symbol,valid:false}]}})),signals=bundles.map(x=>x[1]).filter(s=>s.valid),map=new Map(signals.map(s=>[s.symbol,s]));
  const[positions,openOrders,recentOrders,account]=await Promise.all([alpaca(env,'/v2/positions'),alpaca(env,'/v2/orders?status=open&direction=desc&nested=false'),alpaca(env,'/v2/orders?status=all&limit=500&direction=desc&nested=false'),alpaca(env,'/v2/account')]);
  const actions=await cancelStaleBotOrders(env,openOrders,scheduledTime),inv=botInventoryFromOrders(recentOrders),botPositions=positions.filter(p=>Math.abs(inv[p.symbol]||0)>1e-8),botOpen=new Set(openOrders.filter(isBotOrder).map(o=>o.symbol));
  for(const p of botPositions){if(botOpen.has(p.symbol))continue;const d=exitDecision(p,map.get(p.symbol),env,scheduledTime,etParts);if(d.exit){await placeLimitSell(env,p,map.get(p.symbol),scheduledTime,d.reason);actions.push({action:'sell',symbol:p.symbol,reason:d.reason,pnlPct:round(d.pnlPct,5)});botOpen.add(p.symbol)}}
  const equity=Math.max(1,+account.equity||1),last=Math.max(1,+account.last_equity||equity),draw=Math.min(0,equity/last-1),dd=draw<=-.012?0:draw<=-.008?.35:draw<=-.004?.65:1,perf=calculatePerformanceDetailed(recentOrders),daily=todayPerformance(recentOrders,scheduledTime),learn=adaptiveLearning(perf,num(env.MIN_ENTRY_SCORE,82)),dailyLoss=daily.realizedPnl<=-(equity*pct(env.MAX_DAILY_LOSS_PCT,.01)),lossCount=daily.losses>=int(env.MAX_DAILY_LOSING_EXITS,3),consec=daily.maxConsecLoss>=int(env.MAX_CONSECUTIVE_LOSSES,3),regime=marketRegime(map),cool=recentCooldowns(recentOrders,scheduledTime,int(env.SYMBOL_COOLDOWN_MINUTES,45)),maxSpread=pct(env.MAX_SPREAD_PCT,.0025),maxAge=int(env.MAX_QUOTE_AGE_SECONDS,20),occupied=new Set(botPositions.map(p=>p.symbol)),canRisk=entryWindowOpen(scheduledTime)&&!inConfiguredBlackout(env,scheduledTime)&&!dailyLoss&&!lossCount&&!consec&&dd>0&&!regime.shock,ranked=signals.filter(s=>!occupied.has(s.symbol)&&!botOpen.has(s.symbol)&&!cool.has(s.symbol)&&s.spreadPct<=maxSpread&&s.quoteAgeSec<=maxAge&&!s.chop).sort((a,b)=>Math.max(b.score,b.shortScore)-Math.max(a.score,a.shortScore)),newsBlocked=await recentNewsRisk(env,ranked.slice(0,8).map(s=>s.symbol),scheduledTime),currentExposure=botPositions.reduce((sum,p)=>sum+Math.abs(+p.market_value||0),0),maxPositions=int(env.MAX_CONCURRENT_POSITIONS,2),maxTotal=num(env.MAX_TOTAL_EXPOSURE_USD,10);
  let slots=Math.max(0,maxPositions-botPositions.length),remaining=Math.max(0,maxTotal-currentExposure),techCount=botPositions.filter(p=>TECH.has(p.symbol)).length;const buyingPower=Math.max(0,+account.buying_power||0);
  for(const c of ranked){if(!canRisk||slots<=0||remaining<1)break;if(newsBlocked.has(c.symbol))continue;const longOk=regime.longOk&&c.longConfirmed&&c.score>=learn.scoreThreshold,shortOk=bool(env.SHORTS_ENABLED,false)&&regime.shortOk&&c.shortConfirmed&&c.shortScore>=learn.scoreThreshold;if(!longOk&&!shortOk)continue;if(shortOk)continue;if(TECH.has(c.symbol)&&techCount>=int(env.MAX_TECH_POSITIONS,1))continue;const n=Math.min(positionNotional(equity,c,env,learn,dd),remaining,buyingPower);if(n<1)continue;await placeLimitBuy(env,c,n,scheduledTime);actions.push({action:'buy',symbol:c.symbol,reason:c.s5.pullback?'mtf_pullback':'mtf_breakout',score:round(c.score,1),spreadPct:round(c.spreadPct,5),notional:round(n,2)});slots--;remaining-=n;botOpen.add(c.symbol);if(TECH.has(c.symbol))techCount++}
  const journal={event:'decision_journal',ts:new Date(+scheduledTime).toISOString(),strategy:STRATEGY,feed:snap.feed,universe:symbols,regime,learning:learn,perf:{trades:perf.trades,winRate:round(perf.winRate,3),profitFactor:round(perf.profitFactor,2),expectancy:round(perf.expectancy,4)},daily:{pnl:round(daily.realizedPnl,2),losses:daily.losses,maxConsecLoss:daily.maxConsecLoss},risk:{intradayDrawdown:round(draw,4),drawdownMultiplier:dd,dailyLossGuard:dailyLoss,lossCountGuard:lossCount,consecutiveLossGuard:consec},leaders:ranked.slice(0,6).map(s=>({symbol:s.symbol,score:round(s.score,1),shortScore:round(s.shortScore,1),spread:round(s.spreadPct,5),rvol:round(s.rvol,2),rsi:round(s.rsi,1)})),actions};console.log(JSON.stringify(journal));return{status:actions.length?'acted':'hold',endpoint:'paper',strategy:STRATEGY,feed:snap.feed,universe:symbols,regime,learning:learn,daily:journal.daily,risk:journal.risk,actions,leaders:journal.leaders}
}
const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;'),money=v=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(+v||0),percent=v=>`${((+v||0)*100).toFixed(2)}%`;
async function digest(v){return new Uint8Array(await crypto.subtle.digest('SHA-256',new TextEncoder().encode(v)))}async function secureEqual(l,r){const[a,b]=await Promise.all([digest(l),digest(r)]);if(a.length!==b.length)return false;let d=0;for(let i=0;i<a.length;i++)d|=a[i]^b[i];return d===0}async function auth(req,env){if(!env.DASHBOARD_PASSWORD)return false;const a=req.headers.get('Authorization');if(!a?.startsWith('Basic '))return false;try{const d=atob(a.slice(6)),i=d.indexOf(':');return i>=0&&secureEqual(d.slice(i+1),env.DASHBOARD_PASSWORD)}catch{return false}}
async function dashboard(env){const[o,p,a]=await Promise.all([alpaca(env,'/v2/orders?status=all&limit=500&direction=desc&nested=false'),alpaca(env,'/v2/positions'),alpaca(env,'/v2/account')]),perf=calculatePerformanceDetailed(o),inv=botInventoryFromOrders(o),bp=p.filter(x=>Math.abs(inv[x.symbol]||0)>1e-8),u=bp.reduce((s,x)=>s+(+x.unrealized_pl||0),0),rows=o.filter(isBotOrder).slice(0,40).map(x=>`<tr><td>${esc(x.submitted_at?new Date(x.submitted_at).toLocaleString('en-US',{timeZone:'America/New_York'}):'—')}</td><td>${esc(x.symbol)}</td><td>${esc(String(x.side||'').toUpperCase())}</td><td>${esc(x.type)}</td><td>${esc(x.status)}</td><td>${x.filled_avg_price?money(x.filled_avg_price):'—'}</td></tr>`).join('');return new Response(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="refresh" content="60"><title>Adaptive Paper Trader v8</title><style>:root{color-scheme:dark;--bg:#0b1020;--card:#151c32;--line:#2a3557;--text:#eef3ff;--muted:#9eabc9;--green:#2dd4a8}*{box-sizing:border-box}body{margin:0;background:var(--bg);font:15px system-ui;color:var(--text)}main{max-width:1250px;margin:auto;padding:28px 18px 50px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(185px,1fr));gap:14px}.card{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:17px}.label{color:var(--muted);font-size:12px;text-transform:uppercase}.value{font-size:23px;font-weight:750;margin-top:7px}.small{font-size:13px;color:var(--muted);margin-top:7px}header{display:flex;justify-content:space-between;gap:15px;align-items:center;margin-bottom:22px}.badge{border:1px solid var(--green);color:var(--green);padding:7px 11px;border-radius:999px;font-weight:700}table{width:100%;border-collapse:collapse;background:var(--card);margin-top:22px}th,td{text-align:left;padding:10px;border-bottom:1px solid var(--line)}th{color:var(--muted);font-size:12px}</style></head><body><main><header><div><h1>Alpaca Adaptive Intraday Guard v8</h1><div class="small">Paper-only · 1m/5m/15m confirmation · live quote/spread checks · adaptive risk/exits · dynamic scanner</div></div><div class="badge">${String(env.TRADING_ENABLED)==='true'?'ARMED':'DISABLED'}</div></header><div class="grid"><div class="card"><div class="label">Paper equity</div><div class="value">${money(a.equity)}</div><div class="small">Buying power ${money(a.buying_power)}</div></div><div class="card"><div class="label">Realized bot P&L</div><div class="value">${money(perf.realizedPnl)}</div><div class="small">${perf.trades} completed exits</div></div><div class="card"><div class="label">Open P&L</div><div class="value">${money(u)}</div><div class="small">${bp.length} bot-owned positions</div></div><div class="card"><div class="label">Win rate</div><div class="value">${percent(perf.winRate)}</div><div class="small">${perf.wins} wins / ${perf.losses} losses</div></div><div class="card"><div class="label">Profit factor</div><div class="value">${Number(perf.profitFactor||0).toFixed(2)}</div><div class="small">Expectancy ${money(perf.expectancy)}</div></div><div class="card"><div class="label">Preferred feed</div><div class="value">${esc(preferredFeed(env).toUpperCase())}</div><div class="small">IEX fallback if SIP unavailable</div></div></div><table><thead><tr><th>Time ET</th><th>Symbol</th><th>Side</th><th>Order</th><th>Status</th><th>Fill</th></tr></thead><tbody>${rows||'<tr><td colspan="6">No bot orders yet.</td></tr>'}</tbody></table><div class="small" style="margin-top:16px">Paper simulation only. No strategy can guarantee profit.</div></main></body></html>`,{headers:{'Content-Type':'text/html; charset=utf-8','Cache-Control':'no-store'}})}
const app={async fetch(request,env){const url=new URL(request.url);if(request.method!=='GET')return Response.json({error:'not_found'},{status:404});if(url.pathname==='/api/status')return Response.json({service:'alpaca-paper-guard',strategy:STRATEGY,status:String(env.TRADING_ENABLED)==='true'?'armed':'disabled',endpoint:'paper',features:{dynamicScanner:bool(env.DYNAMIC_SCANNER_ENABLED,true),marketDataFeed:preferredFeed(env),multiTimeframe:['1Min','5Min','15Min'],liveQuoteGuard:true,spreadGuard:true,staleQuoteGuard:true,marketableLimitOrders:true,dynamicRiskSizing:true,dynamicAtrExits:true,regimeFilter:true,chopFilter:true,newsRiskFilter:bool(env.NEWS_RISK_FILTER_ENABLED,true),adaptiveLearning:true,drawdownGovernor:true,decisionJournal:true,botPositionIsolation:true,shortStrategyFramework:bool(env.SHORTS_ENABLED,false)},safeguards:{minEntryScore:num(env.MIN_ENTRY_SCORE,82),maxConcurrentPositions:int(env.MAX_CONCURRENT_POSITIONS,2),maxDailyLossPct:pct(env.MAX_DAILY_LOSS_PCT,.01),maxDailyLosingExits:int(env.MAX_DAILY_LOSING_EXITS,3),maxConsecutiveLosses:int(env.MAX_CONSECUTIVE_LOSSES,3),symbolCooldownMinutes:int(env.SYMBOL_COOLDOWN_MINUTES,45),maxSpreadPct:pct(env.MAX_SPREAD_PCT,.0025),maxQuoteAgeSeconds:int(env.MAX_QUOTE_AGE_SECONDS,20)}},{headers:{'Cache-Control':'no-store'}});if(url.pathname!=='/')return Response.json({error:'not_found'},{status:404});if(!env.DASHBOARD_PASSWORD)return Response.json({error:'dashboard_password_not_configured'},{status:503});if(!await auth(request,env))return new Response('Authentication required',{status:401,headers:{'WWW-Authenticate':'Basic realm="Alpaca Paper Guard"','Cache-Control':'no-store'}});try{return await dashboard(env)}catch(e){console.error(JSON.stringify({event:'dashboard_failed',message:e.message}));return Response.json({error:'dashboard_unavailable'},{status:502})}},async scheduled(controller,env,ctx){ctx.waitUntil(runCycle(env,controller.scheduledTime).then(r=>console.log(JSON.stringify({event:'cycle_complete',...r}))).catch(e=>console.error(JSON.stringify({event:'cycle_failed',message:e.message}))))}};
export default app;
