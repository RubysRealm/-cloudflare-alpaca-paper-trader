import{isBotOrder,etDateKey,clamp}from'./config.js';
export function calculatePerformanceDetailed(orders){const s=[...orders].filter(o=>o.filled_at&&o.filled_avg_price&&isBotOrder(o)).sort((a,b)=>String(a.filled_at).localeCompare(String(b.filled_at))),inv={},pnls=[];let realizedPnl=0,wins=0,losses=0,grossWin=0,grossLoss=0,consecLoss=0,maxConsecLoss=0;for(const o of s){const q=Math.abs(+o.filled_qty),p=+o.filled_avg_price;if(!(q>0&&p>0))continue;const k=o.symbol;inv[k]||={qty:0,cost:0};const x=inv[k];if(o.side==='buy'){x.cost+=q*p;x.qty+=q;continue}if(x.qty<=0)continue;const sold=Math.min(q,x.qty),avg=x.cost/x.qty,pnl=sold*(p-avg);realizedPnl+=pnl;pnls.push(pnl);x.qty-=sold;x.cost-=sold*avg;if(pnl>0){wins++;grossWin+=pnl;consecLoss=0}else if(pnl<0){losses++;grossLoss+=Math.abs(pnl);consecLoss++;maxConsecLoss=Math.max(maxConsecLoss,consecLoss)}if(x.qty<1e-8){x.qty=0;x.cost=0}}const trades=pnls.length,avgWin=wins?grossWin/wins:0,avgLoss=losses?grossLoss/losses:0,expectancy=trades?realizedPnl/trades:0,profitFactor=grossLoss?grossWin/grossLoss:grossWin>0?99:0;return{realizedPnl,trades,wins,losses,winRate:trades?wins/trades:0,grossWin,grossLoss,profitFactor,avgWin,avgLoss,expectancy,maxConsecLoss,pnls}}
export function botInventoryFromOrders(orders){const inv={};for(const o of orders.filter(o=>o.status==='filled'&&isBotOrder(o))){const q=+o.filled_qty||0;inv[o.symbol]=(inv[o.symbol]||0)+(o.side==='buy'?q:-q)}return inv}
export function todayPerformance(orders,now){const key=etDateKey(now);return calculatePerformanceDetailed(orders.filter(o=>o.filled_at&&etDateKey(o.filled_at)===key))}
export function recentCooldowns(orders,now,minutes){const cut=+now-minutes*60000,b=new Set;for(const o of orders){if(o.side!=='sell'||o.status!=='filled'||!isBotOrder(o))continue;const t=Date.parse(o.filled_at||0);if(Number.isFinite(t)&&t>=cut)b.add(o.symbol)}return b}
export function adaptiveLearning(perf,baseScore){if(perf.trades<12)return{scoreThreshold:baseScore,riskMultiplier:.75,state:'learning_sample'};let d=0,r=1;if(perf.profitFactor<.9||perf.expectancy<0){d+=6;r*=.55}else if(perf.profitFactor<1.15){d+=3;r*=.75}else if(perf.profitFactor>1.5&&perf.winRate>.5){d-=2;r*=1.05}if(perf.maxConsecLoss>=3){d+=3;r*=.65}return{scoreThreshold:clamp(baseScore+d,78,94),riskMultiplier:clamp(r,.35,1.05),state:'adaptive'}}
